{
  "name": "plumber-pro-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Anthropic API proxy for Plumber Pro — Cloudflare Worker.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.78.0"
  }
}
